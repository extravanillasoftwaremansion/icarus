{
  "name": "1car.us",
  "version": "0.0.3",
  "description": "A no-dependency Stencil/React alternative written in TypeScript.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "module": "./dist/index.mjs",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "tsup",
    "unpackage": "rm -r dist",
    "build": "bash utils/package-app.sh",
    "dev": "npx live-server dist/src",
    "pub": "npm publish -access=public",
    "deploy": "",
    "semanticdeploy": ""
  },
  "author": "Ashton Morris",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "tsup": "^8.0.1",
    "typescript": "^5.2.2"
  }
}
