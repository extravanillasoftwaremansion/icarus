{
  "name": "1car.us",
  "version": "0.0.1",
  "description": "A no-dependency Stencil/React alternative written in TypeScript.",
  "main": "dist/src/decorators/index.js",
  "types": "dist/src/decorators/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bash ./utils/deploy.sh",
    "build:go": "bash ./utils/build-go.sh",
    "destroy:build": "rm -r dist",
    "dev": "npx live-server ./dist/src",
    "package": "bash ./utils/package-app.sh",
    "unpackage": "rm -r dist",
    "install:go": "npx ts-node utils/install-wasm-go.ts",
    "uninstall:go": "npx ts-node utils/uninstall-wasm-go.ts",
    "compile:go": "npx ts-node utils/compile-go.ts",
    "destroy:go": "bash ./utils/destroy-go.sh",
    "pub": "npm publish -access=public",
    "deploy": "",
    "semanticdeploy": ""
  },
  "author": "Ashton Morris",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "lite-server": "^2.6.1",
    "prettier": "^3.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "1car.us": "^0.0.1"
  }
}
