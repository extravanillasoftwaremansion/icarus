{
  "name": "component",
  "version": "0.0.1",
  "description": "A no-dependency Stencil/React alternative written in TypeScript.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "bash ./utils/package-app.sh",
    "unpackage": "rm -r dist",
    "dev": "npx live-server ./dist/src",
    "deploy": "",
    "semanticdeploy": ""
  },
  "author": "Ashton Morris",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "lite-server": "^2.6.1",
    "typescript": "^5.2.2"
  }
}
