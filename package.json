{
  "name": "component",
  "version": "0.0.1",
  "description": "A no-dependency Stencil/React alternative written in TypeScript.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx live-server ./dist",
    "compile": "tsc",
    "package": "bash ./utils/package-app.sh",
    "prepare": "npm run package && npm run compile"
  },
  "author": "Ashton Morris",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "lite-server": "^2.6.1",
    "typescript": "^5.2.2"
  }
}
